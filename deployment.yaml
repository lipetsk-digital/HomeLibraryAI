apiVersion: apps/v1
kind: Deployment
metadata:
  name: homelibrary-bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: homelibrary-bot
  template:
    metadata:
      labels:
        app: homelibrary-bot
    spec:
      containers:
      - name: bot
        image: cr.selcloud.ru/stkorn/homelibrary-bot:latest
        env:
        - name: TELEGRAM_TOKEN
          valueFrom:
            secretKeyRef:
              name: homelibrary-bot-secrets
              key: telegram-token
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"